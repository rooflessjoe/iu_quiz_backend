{
    "quiz_list": "'SELECT * FROM quiz'",
    "quiz": "'SELECT * FROM quiz WHERE quizid = $1 AND quizname = $2', [req.query.quizID, req.query.quizName]",
    "data": "'SELECT name, email FROM users'"
}